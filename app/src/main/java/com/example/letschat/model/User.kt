package com.example.letschat.model
// The User data class is serving as a model i.e a data base a structure we can use to provide multiple users  for this project cloning since we don't have contacts
data class User(
    val userName:String,
    val status:String
)
